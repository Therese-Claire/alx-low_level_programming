#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 *@n : function parameter to store the value input
 *
 * Return: The absolute value of the input integer.
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
