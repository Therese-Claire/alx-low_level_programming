#include "main.h"

/**
 * add - function adds two interger numbers
 * @a:First integer.
 * @b: Second integer
 *
 * Return: Returns res after add(a + b).
 */
int add(int a, int b)
{
	int res;

	res = a + b;
	return (res);
}
